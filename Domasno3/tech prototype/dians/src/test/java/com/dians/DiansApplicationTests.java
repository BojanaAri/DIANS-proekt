package com.dians;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Indicates that the class is a Spring Boot test class
@SpringBootTest
class DiansApplicationTests {

    // Simple test method to check if the application context loads successfully
    @Test
    void contextLoads() {
        // The body of the test method is empty as it's just checking if the context loads
    }
}
